# Copy this file to the project root as `.env` and fill with your values.
# Example DATABASE_URL for async SQLAlchemy + asyncpg:
DATABASE_URL=postgresql+asyncpg://postgres:1810@localhost:5432/upcycle_db

# Optional individual parts (if you prefer building the URL yourself):
POSTGRES_USER=postgres
POSTGRES_PASSWORD=1810
POSTGRES_DB=upcycle_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Note: If using a hosted Postgres (Supabase, Heroku, AWS RDS, Azure DB, ElephantSQL),
# copy the provider's connection string into `DATABASE_URL`.
